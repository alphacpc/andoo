import 'package:flutter/material.dart';

class CultureScreen extends StatelessWidget {
  const CultureScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          child: Text("screen Ethnies et culture"),
      ),
    );
  }
}
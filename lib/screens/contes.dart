import 'package:flutter/material.dart';

class ConteScreen extends StatelessWidget {
  const ConteScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          child: Text("screen contes"),
      ),
    );
  }
}